# Результат локального прогона кейса 1

Проверено 23.09.2026. Последний прогон запущен в 13:29:18 МСК,
все результаты получены к 13:31:54 МСК.

Параметры: `acc-3`, `ratePerSecond=11`, `testCount=1200`,
`processingTimeMillis=80000`. Ограничения провайдера: 10 запросов/с,
30 параллельных запросов, средняя обработка 1 с, стоимость вызова 30.

| Показатель | Результат |
| --- | ---: |
| Завершённые покупки | 1200 |
| SUCCESS | **1200 (100%)** |
| FAIL / ERROR | **0 / 0** |
| Вызовы провайдера | 1200 |
| Успешные вызовы провайдера | 1200 |
| Нарушения RPS | **0** |
| Другие отказы провайдера, включая параллельность | **0** |
| Доход, `payments_amount_total` | 495247 |
| Расход, `external_amount_total` | 36000 |
| Доход / расход в процентах от их суммы | **93,2235% / 6,7765%** |
| Средняя длительность успешной покупки | 18,833 с |
| Поминутный процент успеха в Prometheus | 100% во всех 14 доступных точках |

Количество покупок, вызовы и суммы рассчитаны как разность счётчиков
Bombardier до и после последнего прогона. Предыдущий диагностический прогон
в эти значения не включён. В Grafana запросы с `increase()` могут давать
немного отличающиеся числа из-за интервалов сбора и экстраполяции.

В первом диагностическом прогоне отправка с интервалом 101 мс дала
6 отказов по RPS. Итоговое решение использует 110 мс для лимита 10 RPS;
повторный полный прогон выше прошёл без отказов. В Prometheus также убран
повторный сбор того же Bombardier через второй адрес.

Проверка кода: **10 тестов, 0 ошибок, 0 падений**, Maven BUILD SUCCESS.
Собран исполняемый `target/demo-0.0.1-SNAPSHOT.jar`.

[Дашборд этого прогона](http://localhost:3000/d/KVr-Vmpnz/services-statistic?orgId=1&from=1790159350000&to=1790159550000&var-service=smth_decent).
Grafana: `admin` / `quipy`. Разверни строки General и Tests info.
Данные доступны, пока сохранено локальное хранилище Prometheus.

Приложение после проверки остановлено, чтобы освободить порт 8081 для IDEA.
Контейнеры Postgres, Bombardier, Prometheus и Grafana оставлены запущенными.
Для остановки стенда: `docker compose stop` из корня проекта.

Локальные KPI выполнены. Удалённый прогон и сдача ментору не выполнялись;
их запуск описан в [CASE1.md](CASE1.md).
